# Constitution Compliance — FR-165

Generated 2026-05-10 after the build phase shipped.

## Files modified or created

| File                                           | Status   | Lines | Purpose                                                                                               |
| ---------------------------------------------- | -------- | ----- | ----------------------------------------------------------------------------------------------------- |
| `LICENSE-DEVPILOT`                             | created  | +182  | J1 — BSL 1.1 license file (header + verbatim Notice/Terms/Covenants from canonical source) + 54 paths |
| `docs/repo-split-plan.md`                      | created  | +149  | J3 — Path list of truth + cutover entrance criteria + rollback procedure + J4 dashboard walkthrough   |
| `scripts/devpilot-readiness.ts`                | created  | +105  | J2 — Gateway-coverage verifier; regression-only failure mode; `--update-baseline` flag                |
| `scripts/lib/devpilot-readiness-baseline.json` | created  | +5    | J2 — Initial baseline: 3/27 routes wrapped (11.1%)                                                    |
| `scripts/extract-devpilot-repo.sh`             | created  | +98   | J3 — git filter-repo wrapper; produces sibling staging clone with smoke check                         |
| `scripts/devpilot-split-paths.txt`             | created  | +54   | J3 — Derived path list (regenerated by extract-devpilot-repo.sh from repo-split-plan.md)              |
| `scripts/verify-devpilot-split.ts`             | created  | +127  | J6 — Aggregate verifier (readiness + license-coverage + extraction-dryrun)                            |
| `apps/web/src/lib/devpilot-sdk.ts`             | created  | +63   | J5 — SDK indirection wrapper; re-exports productApi/pipelineApi/featureApi/devpilotApi + helpers      |
| `package.json`                                 | modified | +2    | Wires `pnpm verify:devpilot-readiness` and `pnpm verify:devpilot-split`                               |
| `.env.example`                                 | modified | +7    | Documents `VITE_DEVPILOT_BASE_URL` (UNSET in normal use; cutover-only)                                |
| `specs/165-devpilot-repo-cloud-split/*`        | spec     | —     | spec/plan/research/tasks/data-model/quickstart + 1 contract                                           |

## Constitution gate results

| Principle                                  | Outcome | Notes                                                                                                                                                                                                                  |
| ------------------------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **II. TypeScript-First (Strict Mode)**     | PASS    | No `any`. `Baseline`, `CheckResult`, helper types explicit. `pnpm tsc -b` clean.                                                                                                                                       |
| **VI. SOLID & file-size limits**           | PASS    | Largest new file: LICENSE-DEVPILOT 182 lines (legal text). All TS files under 130 lines. Bash script 98 lines. No new constitutional debt.                                                                             |
| **VIII. Security Engineering**             | PASS    | No new tables, no new RLS surface area, no auth changes. SDK wrapper deliberately preserves existing apiClient auth flow (re-exports unchanged objects).                                                               |
| **IX. Performance Engineering**            | PASS    | No runtime impact on existing paths. Verifier scripts are ad-hoc tooling (CI-time only). SDK wrapper is a pure re-export — zero overhead until cutover-time when callers migrate.                                      |
| **XI. Verification-Driven Implementation** | PASS    | Each task verified before moving on: T002 baseline run + sanity check, T005 syntax check, T009 typecheck, T011 aggregate run, T012 7-FR regression all exit 0.                                                         |
| **III. API-First**                         | PASS    | Verifier semantics pinned in `contracts/license-coverage-contract.md` before code. `devpilot-sdk.ts` documents the cutover contract via JSDoc.                                                                         |
| **Backwards compatibility**                | PASS    | `apps/web/src/lib/devpilot-sdk.ts` is purely additive — existing call sites import from `./api/...` continue to work unchanged. `VITE_DEVPILOT_BASE_URL` defaults to undefined; legacy behaviour preserved when unset. |
| **No destructive changes (v1.0 scope)**    | PASS    | Zero migrations, zero table drops, zero existing-file refactors. Staging cloud resources documented as operator-action items, not auto-executed. Cutover (FR-165 v1.1) explicitly out-of-scope.                        |

## Manual sign-off (build-stage)

All 7 test_cases (TC-165-01 through TC-165-07) verified by hand-run smokes during the build:

- **TC-165-01**: `LICENSE-DEVPILOT` exists at repo root; first 25 lines confirm BSL 1.1 header with Licensor=Iones Walter, Change Date=May 10, 2030, Change License=Apache 2.0; 54 path entries enumerated (matches the table in `docs/repo-split-plan.md`).
- **TC-165-02**: `pnpm verify:devpilot-readiness` reports `3/27 (11.1%)` baseline + lists wrapped (`pipeline-status`, `promote-memory-row`, `usage-rollup`) and unwrapped routes; PASS.
- **TC-165-03**: `docs/repo-split-plan.md` contains all four sections (Path list, Cutover entrance criteria, Out of scope for v1.0, Rollback procedure) plus J4 manual provisioning steps; 54 enumerated paths.
- **TC-165-04**: `scripts/extract-devpilot-repo.sh` syntax-validated (`bash -n`); pre-flight check rejects missing `git-filter-repo`; documented usage. Full extraction not run in build (creates sibling dir; ran via verify-devpilot-split's `--analyze` dry-run instead).
- **TC-165-05**: J4 staging cloud resources are operator-action TODOs documented in `docs/repo-split-plan.md` § "Manual provisioning steps for J4" with step-by-step Supabase + DigitalOcean dashboard walkthroughs. Build ships docs-only; provisioning happens out-of-band.
- **TC-165-06**: `apps/web/src/lib/devpilot-sdk.ts` exports `productApi`, `pipelineApi` (renamed from `pipelineApiMethods`), `featureApi` (renamed from `featureApiMethods`), `devpilotApi` + helpers `getDevpilotBaseUrl()`, `isDevpilotOverrideActive()`, `devpilotFetch()`. `pnpm tsc -b` exits 0.
- **TC-165-07**: `pnpm verify:devpilot-split` reports a single `PASS — DevPilot split readiness verified` with all 3 sub-checks green (readiness no-regression; license-coverage 54 paths consistent; extraction-dryrun analyze succeeded after `brew install git-filter-repo`).

## Operator action remaining (post-build, pre-cutover)

Per [docs/repo-split-plan.md](../../docs/repo-split-plan.md), J4 staging provisioning requires dashboard access:

1. **Supabase project** — create `ai-devpilot-staging` in SYD1; replay migrations; verify RLS 35/35; record IDs in plan doc.
2. **DigitalOcean App** — create `ai-devpilot-staging` static site in SYD1; default URL only (no DNS); record App ID.

These are tracked in `## Provisioning record (J4)` of the plan doc with `<pending>` placeholders.

## No new violations to report

Constitution validator exit 0 on all 10 build files. No `any` types introduced. No file-size or function-size limits exceeded. No new RLS-scoped tables (verifier scope set unchanged at 35).

## Pre-existing tech debt acknowledged (carry-over)

- `scripts/verify-rls-status.ts` (511 lines, allowlisted in FR-164 with TBD-FR for decomposition) — **unchanged** in this build.
